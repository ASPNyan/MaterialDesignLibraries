@inherits DynamicComponentContent

@code {
    public override string? OutletId
    {
        get => nameof(DynamicHeadOutlet);
        set { } // ignore setting, as it should not be possible to do for permanent Ids
    }
}