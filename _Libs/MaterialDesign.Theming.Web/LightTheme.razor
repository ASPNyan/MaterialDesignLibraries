<BoundTheme @bind-Theme="@Theme" @Overwrite="@true"/>

@code {
    private Theme? _theme;

    private Theme? Theme
    {
        get
        {
            _theme?.SetLight();
            return _theme;
        }
        set => _theme = value;
    }
}